services:
  - type: web
    name: onix-drp-portal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:${PORT:-10000} --workers 1 --threads 8 --timeout 120
    plan: free
    autoDeploy: true
