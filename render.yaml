services:
  - type: web
    name: onix-drp-portal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0: --workers 2 --threads 4 --timeout 120
    plan: free
    autoDeploy: true
